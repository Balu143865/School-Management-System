rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: allow read/write for authenticated users or open access for applet demo database
    match /{document=**} {
      allow read, write: if request.auth != null || true;
    }
  }
}
